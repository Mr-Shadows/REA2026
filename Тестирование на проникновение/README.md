# 🚀 REA2026 Pentest Arsenal

> **Фокус:** Максимальная скорость → поиск флагов → сдача в сервис. Никаких отчётов — только флаги.  
> **Версия:** 1.0 | **Дата:** 2026 | **Автор:** Подготовлено для REA2026

---

## 📌 Структура арсенала

| Файл | Назначение | Когда открывать |
|------|------------|-----------------|
| [`quick_start.md`](quick_start.md) | Стратегия 7 часов + чек-листы + скрипты старта | **ПЕРВЫМ ДЕЛОМ** — до старта модуля |
| [`cheatsheet_tools.md`](cheatsheet_tools.md) | Все команды утилит (nmap, hydra, sqlmap...) | Во время работы — копировать и вставлять |
| [`vulnerabilities_services.md`](vulnerabilities_services.md) | Слабые места сервисов (Grafana, FreeIPA, SMB...) | При обнаружении сервиса на цели |
| [`linux_windows_pentest.md`](linux_windows_pentest.md) | Тактика проникновения и эскалации | После получения шелла |
| [`flag_hunting.md`](flag_hunting.md) | Поиск флагов (универсальные команды + скрипты) | После получения доступа к системе |

---

## ⚡ Быстрый старт (сделать ДО старта модуля)

```bash
# 1. Создать структуру директорий
mkdir -p ~/flags/{target1,target2}/scans

# 2. Добавить алиасы в ~/.bashrc
cat >> ~/.bashrc << 'EOF'

# Pentest aliases
alias findflag='grep -r "RS{" /home /root /opt /var/www /tmp 2>/dev/null | grep -v "proc\|sys\|dev\|run" | head -30'
alias nmapfast='sudo nmap -p- -T4 --min-rate 3000'
alias ffufweb='ffuf -w /usr/share/seclists/Discovery/Web-Content/common.txt -u'
EOF
source ~/.bashrc

# 3. Запустить tmux с 2 панелями (левая — цель 1, правая — цель 2)
tmux new-session -d -s pentest \; split-window -h \; attach-session -t pentest

# 4. В левой панели запустить слушатель:
nc -nvlp 4444

# 5. В правой панели запустить слушатель:
nc -nvlp 4445

# 6. Открыть все файлы в редакторе (для быстрого копирования)
mousepad ~/flags/*.md &
```

---

## 🧭 Навигация во время модуля

### Сценарий 1: Только начал работу
1. Откройте [`quick_start.md`](quick_start.md) → выполните **Чек-лист: Первые 15 минут**
2. Запустите сканирование обеих целей параллельно
3. Откройте [`vulnerabilities_services.md`](vulnerabilities_services.md) → проверьте анонимный доступ к каждому открытому порту

### Сценарий 2: Нашли веб-сервер на 80 порту
1. Откройте [`vulnerabilities_services.md`](vulnerabilities_services.md) → раздел **Веб-приложения**
2. Выполните обязательные проверки: `robots.txt`, `.git/HEAD`, `.env`
3. Запустите `ffuf` из [`cheatsheet_tools.md`](cheatsheet_tools.md)
4. Если найдена уязвимость → перейдите к [`linux_windows_pentest.md`](linux_windows_pentest.md) для получения шелла

### Сценарий 3: Получили шелл на Linux
1. Выполните базовую разведку из [`linux_windows_pentest.md`](linux_windows_pentest.md)
2. Запустите универсальный поиск флагов:
   ```bash
   findflag
   ```
3. Если флаг не найден → проверьте эскалацию через SUID/капабилити в [`linux_windows_pentest.md`](linux_windows_pentest.md)
4. После нахождения флага — **сразу сдайте в сервис** (не ждите конца!)

### Сценарий 4: Застопорились на одной цели >45 минут
1. Переключитесь на вторую цель
2. Проверьте [`vulnerabilities_services.md`](vulnerabilities_services.md) → часто флаги лежат на поверхности (анонимный SMB/FTP/Grafana)
3. Вернитесь к первой цели только после сбора флагов со второй

---

## ⚠️ Критически важные правила

| Правило | Почему важно |
|---------|--------------|
| **Сдавайте флаги сразу** | Сервис может закрыться за 5 минут до конца — не успеете сдать |
| **Не удаляйте файлы на цели** | `rm -rf` может удалить флаги или сломать сервис |
| **Проверяйте анонимный доступ ПЕРВЫМ делом** | 60% флагов в учебных сценариях доступны без пароля |
| **Максимум 45 минут на одну уязвимость** | Лучше собрать 3 простых флага, чем 1 сложный |
| **Флаг ≠ конец** | После получения флага проверьте: нет ли в нём подсказки к следующему? |

---

## 🚀 Готовые скрипты (все в `~/flags/`)

| Скрипт | Назначение | Использование |
|--------|------------|---------------|
| `quick_start.sh` | Автоматический старт: сканирование + слушатели | `bash ~/flags/quick_start.sh` |
| `find_flag.sh` | Универсальный поиск флагов на Linux | `bash ~/flags/find_flag.sh` |
| `anon_check.sh` | Проверка анонимного доступа ко всем сервисам | `bash ~/flags/anon_check.sh 10.255.44.100` |

---

## 💡 ТОП-3 совета от победителей

1. **Анонимный доступ — ваш главный друг.** Проверяйте его для КАЖДОГО сервиса (SMB, FTP, LDAP, Grafana) — это 60% флагов.
2. **Не эскалируйте ради эскалации.** Если флаг доступен как `www-data` — берите его и ищите следующий. Эскалация нужна ТОЛЬКО если флаг недоступен из текущего контекста.
3. **Сдавайте флаги параллельно с поиском.** Не ждите конца — как только нашли, сразу вставляйте в сервис. Меньше риска потерять из-за технических проблем.

---

## 📜 Этическое напоминание

> ✅ Все техники применяются **ТОЛЬКО** в авторизованной лабораторной среде REA2026.  
> ✅ Инструменты используются **ТОЛЬКО** для обучения и демонстрации навыков.  
> ❌ Применение этих методов против реальных систем без письменного разрешения — уголовное преступление (ст. 272.1 УК РФ).

---

## 🔗 Быстрые ссылки

- [Стратегия и чек-листы](quick_start.md)
- [Команды утилит](cheatsheet_tools.md)
- [Слабые места сервисов](vulnerabilities_services.md)
- [Тактика проникновения](linux_windows_pentest.md)
- [Поиск флагов](flag_hunting.md)

---

> 🌟 **Финальный совет:** В соревнованиях без отчёта побеждает не тот, кто нашёл самую сложную уязвимость, а тот, кто **быстрее всех собрал максимальное количество флагов**. Фокус на скорости, автоматизации и типичных местах хранения флагов.

**Удачи на модуле!** 💪🐧
```

---

## 📥 Как развернуть арсенал за 2 минуты (перед стартом модуля)

```bash
# 1. Создать структуру
mkdir -p ~/flags/{target1,target2}/scans

# 2. Сохранить все файлы (выполнить ПОСЛЕДОВАТЕЛЬНО в терминале)
cat > ~/flags/README.md << 'EOF'
[ВСТАВЬТЕ СОДЕРЖИМОЕ README.md ИЗ ВЫШЕ]
EOF

cat > ~/flags/quick_start.md << 'EOF'
[ВСТАВЬТЕ СОДЕРЖИМОЕ quick_start.md]
EOF

cat > ~/flags/cheatsheet_tools.md << 'EOF'
[ВСТАВЬТЕ СОДЕРЖИМОЕ cheatsheet_tools.md]
EOF

cat > ~/flags/vulnerabilities_services.md << 'EOF'
[ВСТАВЬТЕ СОДЕРЖИМОЕ vulnerabilities_services.md]
EOF

cat > ~/flags/linux_windows_pentest.md << 'EOF'
[ВСТАВЬТЕ СОДЕРЖИМОЕ linux_windows_pentest.md]
EOF

cat > ~/flags/flag_hunting.md << 'EOF'
[ВСТАВЬТЕ СОДЕРЖИМОЕ flag_hunting.md]
EOF

# 3. Добавить алиасы
echo "alias findflag='grep -r \"RS{\" /home /root /opt /var/www /tmp 2>/dev/null | grep -v \"proc\|sys\|dev\|run\" | head -30'" >> ~/.bashrc
echo "alias nmapfast='sudo nmap -p- -T4 --min-rate 3000'" >> ~/.bashrc
source ~/.bashrc

# 4. Запустить tmux + слушатели (вручную в терминале):
tmux new-session -d -s pentest \; split-window -h \; attach-session -t pentest
# В левой панели: nc -nvlp 4444
# В правой панели: nc -nvlp 4445