# ⚡ Quick Start: Стратегия 7 часов без отчёта

> Сохраните в `~/flags/quick_start.md`

---

## 🕐 План распределения времени (2 независимые цели)

| Время | Действие | Приоритет |
|-------|----------|-----------|
| **0:00–0:15** | `nmap -sn` обеих целей + запуск полного сканирования в фоне | 🔴 КРИТИЧЕСКИЙ |
| **0:15–0:45** | Анализ открытых портов → выбор самой простой точки входа | 🔴 КРИТИЧЕСКИЙ |
| **0:45–2:30** | Эксплуатация Цель #1 → получение шелла → поиск флагов | 🔴 КРИТИЧЕСКИЙ |
| **2:30–4:00** | Эксплуатация Цель #2 (параллельно с поиском флагов на #1) | 🔴 КРИТИЧЕСКИЙ |
| **4:00–6:30** | Эскалация + глубокий поиск флагов на обеих целях | 🟠 ВЫСОКИЙ |
| **6:30–7:00** | Финальная проверка + сдача ВСЕХ найденных флагов | 🟢 ОБЯЗАТЕЛЬНО |

> 💡 **Правило 30/30:** 30 минут на эксплуатацию → если нет шелла, 30 минут на другую уязвимость → если нет результата — переход к второй цели.

---

## ✅ Чек-лист: Первые 15 минут

- [ ] Определил свою подсеть: `ip a | grep "inet " | grep -v "127.0.0.1"`
- [ ] Выполнил `nmap -sn 10.255.44.0/24 -oG hosts.txt`
- [ ] Запустил полное сканирование портов в фоне для обеих целей:
  ```bash
  sudo nmap -p- -T4 --min-rate 3000 10.255.44.100 -oA target1_full &
  sudo nmap -p- -T4 --min-rate 3000 10.255.44.200 -oA target2_full &
  ```
- [ ] Открыл браузер и проверил веб на обеих целях (80/443/3000/8080)
- [ ] Проверил `robots.txt`, `.git/HEAD`, `.env` на веб-серверах
- [ ] Запустил `nc -nvlp 4444` и `nc -nvlp 4445` для реверс-шеллов
- [ ] Создал директорию `~/flags/{target1,target2}`

---

## 🔓 Чек-лист: Поиск точек входа (выполнять для КАЖДОГО открытого порта)

- [ ] **Порт 21 (FTP)** — проверил анонимный доступ (`ftp $IP` → `anonymous:anonymous`)
- [ ] **Порт 22 (SSH)** — запустил брутфорс с `P@ssw0rd2026`
- [ ] **Порт 80/443 (Веб)** — проверил `robots.txt`, `.git/`, запустил `ffuf`
- [ ] **Порт 3000 (Grafana)** — проверил анонимный доступ к дашбордам
- [ ] **Порт 389 (LDAP)** — проверил анонимный поиск через `ldapsearch -x`
- [ ] **Порт 445 (SMB)** — проверил анонимный доступ через `smbclient -L //$IP -N`
- [ ] **Порт 3306/5432 (БД)** — проверил анонимный доступ (`mysql -h $IP -u root`)
- [ ] Для каждого сервиса — проверил стандартные пароли (`admin:admin`, `P@ssw0rd2026`)

---

## 🚩 Чек-лист: После получения шелла

- [ ] Выполнил базовую разведку:
  ```bash
  id; hostname; uname -a; cat /etc/os-release  # Linux
  whoami /all; systeminfo                      # Windows
  ```
- [ ] Запустил универсальный поиск флагов:
  ```bash
  grep -r "RS{" /home /root /opt /var/www /tmp 2>/dev/null | head -20  # Linux
  findstr /si "RS{" C:\*.* 2>nul                                      # Windows
  ```
- [ ] Проверил неочевидные места:
  - Переменные окружения (`env` / `set`)
  - История команд (`.bash_history` / PowerShell history)
  - `.git/` репозитории
  - Базы данных
- [ ] Если флаг не найден — начал эскалацию привилегий
- [ ] Как только нашёл флаг — сразу сдал в сервис (не ждать конца!)

---

## 🏆 Чек-лист: Последние 30 минут

- [ ] Запустил `~/flags/find_flag.sh` на обеих целях
- [ ] Проверил неочевидные места: `.bash_history`, `.git/`, переменные окружения, реестр (Windows)
- [ ] Сдал ВСЕ найденные флаги в сервис
- [ ] Не тратил время на эскалацию, если флаги уже найдены
- [ ] Убедился, что все флаги скопированы правильно (без лишних пробелов/символов)

---

## 💡 ТОП-5 советов для максимального количества флагов

1. **Анонимный доступ — первым делом.** 60% флагов в учебных сценариях доступны без пароля (SMB, FTP, Grafana, Jenkins).
2. **Не эскалируйте ради эскалации.** Если нашли флаг как `www-data` — берите его и ищите следующий. Эскалация нужна ТОЛЬКО если флаг недоступен из текущего контекста.
3. **Проверяйте `.git/` на веб-серверах.** `git log` → `git show <commit>` часто содержит флаги в коммитах.
4. **Ищите флаги в неочевидных местах:** переменные окружения (`env`), история команд (`cat ~/.bash_history`), метаданные файлов (`exiftool`).
5. **Сдавайте флаги ПАРАЛЛЕЛЬНО с поиском.** Не ждите конца — как только нашли, сразу вставляйте в сервис. Меньше риска потерять.

---

## 🚫 Что НЕ делать (распространённые ошибки)

| Действие | Почему плохо | Альтернатива |
|----------|--------------|--------------|
| Тратить >45 минут на одну уязвимость без результата | Потеря времени → меньше флагов | Переключиться на другую цель/уязвимость |
| Удалять файлы на цели (`rm -rf`) | Можно удалить флаги | Только чтение (`cat`, `grep`) |
| Использовать `msfconsole` вслепую | «Ломает» сервисы → блокировка | Сначала ручная проверка |
| Игнорировать анонимный доступ | Упускаете 50% флагов | Проверять ПЕРВЫМ делом |
| Не проверять соседние подсети | Флаг может быть на другом хосте | `nmap -sn 10.255.43.0/24 10.255.45.0/24` |

---

## ⚡ Готовый скрипт для быстрого старта (сохраните как `~/flags/quick_start.sh`)

```bash
#!/bin/bash
echo "========================================"
echo " QUICK START — REA2026 Pentest"
echo "========================================"

# Шаг 1: Определение подсети
echo "[*] Определение подсети..."
MY_IP=$(ip -4 addr show eth0 | grep -oP '(?<=inet\s)\d+(\.\d+){3}')
SUBNET=$(echo $MY_IP | cut -d'.' -f1-3)
echo "    Мой IP: $MY_IP"
echo "    Подсеть: $SUBNET.0/24"

# Шаг 2: Обнаружение хостов
echo -e "\n[*] Обнаружение хостов в $SUBNET.0/24..."
sudo nmap -sn $SUBNET.0/24 -oG hosts.txt > /dev/null 2>&1
TARGETS=$(grep "Up" hosts.txt | awk '{print $2}' | grep -v "$MY_IP")
echo "    Найдены хосты: $TARGETS"

# Шаг 3: Запуск сканирования в фоне
echo -e "\n[*] Запуск полного сканирования портов..."
for TARGET in $TARGETS; do
  echo "    → $TARGET"
  sudo nmap -p- -T4 --min-rate 3000 $TARGET -oA scans/full_$TARGET > /dev/null 2>&1 &
done
echo "    Сканирование запущено в фоне (ждите 10-15 минут)"

# Шаг 4: Запуск слушателей для реверс-шеллов
echo -e "\n[*] Запуск слушателей для реверс-шеллов..."
nc -nvlp 4444 &
nc -nvlp 4445 &

# Шаг 5: Создание структуры директорий
echo -e "\n[*] Создание структуры директорий..."
mkdir -p ~/flags/{target1,target2}/scans

echo "========================================"
echo " ГОТОВО К РАБОТЕ!"
echo "========================================"
echo "Следующие шаги:"
echo "  1. Проверить веб на портах 80/443/3000"
echo "  2. Проверить анонимный доступ (SMB/FTP/LDAP)"
echo "  3. Через 10 минут проверить результаты nmap:"
echo "     grep open scans/full_*.gnmap"
```

Использование:
```bash
chmod +x ~/flags/quick_start.sh
bash ~/flags/quick_start.sh
```
```

---

## 📥 Как подготовиться перед стартом модуля

1. **Создайте директорию:**
```bash
mkdir -p ~/flags/{target1,target2}/scans
```

2. **Сохраните все 5 файлов:**
```bash
nano ~/flags/cheatsheet_tools.md      # Вставьте содержимое файла 1
nano ~/flags/vulnerabilities_services.md  # Файл 2
nano ~/flags/linux_windows_pentest.md     # Файл 3
nano ~/flags/flag_hunting.md              # Файл 4
nano ~/flags/quick_start.md               # Файл 5
```

3. **Добавьте алиасы в `~/.bashrc`:**
```bash
echo "alias findflag='grep -r \"RS{\" /home /root /opt /var/www /tmp 2>/dev/null | head -30'" >> ~/.bashrc
echo "alias nmapfast='sudo nmap -p- -T4 --min-rate 3000'" >> ~/.bashrc
source ~/.bashrc
```

4. **Запустите `tmux` с 2 панелями:**
```bash
tmux new-session -d -s pentest
tmux split-window -h
tmux attach-session -t pentest
```

5. **Запустите слушатели:**
```bash
# В левой панели:
nc -nvlp 4444

# В правой панели:
nc -nvlp 4445
```