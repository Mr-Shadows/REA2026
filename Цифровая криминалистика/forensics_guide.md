```markdown
# 🕵️‍♂️ Полное руководство по цифровой форензике: Расследование образов дисков и флеш-накопителей

> **Важно:** Этот гайд предназначен для использования на соревнованиях по цифровой форензике. Все действия выполняйте в режиме **только для чтения (read-only)** для сохранения целостности доказательств.

---

## 📌 Содержание

- [📌 Общие правила и подготовка](#-общие-правила-и-подготовка)
- [🔍 Этап 1: Анализ структуры образа](#-этап-1-анализ-структуры-образа)
- [💽 Этап 2: Извлечение файловой системы](#-этап-2-извлечение-файловой-системы)
- [🧩 Этап 3: Сбор артефактов по ОС](#-этап-3-сбор-артефактов-по-ос)
- [🕳️ Этап 4: Поиск скрытых и удалённых данных](#️-этап-4-поиск-скрытых-и-удалённых-данных)
- [⏱️ Этап 5: Построение таймлайна событий](#️-этап-5-построение-таймлайна-событий)
- [🌐 Этап 6: Сетевая форензика](#-этап-6-сетевая-форензика)
- [💾 Этап 7: Анализ памяти (кратко)](#-этап-7-анализ-памяти-кратко)
- [🔌 Этап 8: Работа с флеш-накопителями](#-этап-8-работа-с-флеш-накопителями)
- [📝 Этап 9: Оформление отчёта](#-этап-9-оформление-отчёта)
- [✅ Чек-листы и шпаргалки](#-чек-листы-и-шпаргалки)
- [🛠️ Рекомендуемые инструменты](#️-рекомендуемые-инструменты)
- [💡 Заключение: 5 правил победителя](#-заключение-5-правил-победителя)

---

## 📌 Общие правила и подготовка

### Критически важные шаги перед началом

```bash
# 1. Проверка хеш-суммы оригинального образа
sha256sum RS2026_F.vdi
# Сравните с эталонным хешем от организаторов

# 2. Создание рабочей копии (НИКОГДА не работайте с оригиналом!)
cp RS2026_F.vdi RS2026_F_work.vdi

# 3. Конвертация VDI → RAW (для совместимости с инструментами)
qemu-img convert -O raw RS2026_F_work.vdi disk.img

# 4. Проверка целостности рабочей копии
sha256sum disk.img > disk.img.sha256
```

### ⚠️ Запрещённые действия
- ❌ Работа напрямую с оригинальным образом или флешкой
- ❌ Форматирование флеш-накопителей до окончания модуля "D"
- ❌ Удаление сжатых образов с флешек
- ❌ Отключение автосохранения в редакторе отчёта
- ❌ Сохранение отчёта на рабочем столе виртуальной машины (только физический ПК!)

---

## 🔍 Этап 1: Анализ структуры образа

### Шаг 1.1: Определение типа образа

```bash
file disk.img
```

Пример вывода:
```
disk.img: DOS/MBR boot sector; partition 1 : ID=0x7, active, start-CHS (0x0,32,33), end-CHS (0x3ff,254,63), startsector 2048, 409600 sectors
```

### Шаг 1.2: Анализ разделов

```bash
# С помощью Sleuth Kit (рекомендуется)
mmls disk.img
```

Пример вывода:
```
     Slot    Start        End          Length       Description
000:  Meta    0000000000   0000000000   0000000001   Primary Table (#0)
001:  -----   0000000000   0000002047   0000002048   Unallocated
002:  000:000 0000002048   0000411647   0000409600   NTFS (0x07)      ← Windows!
003:  -----   0000411648   0000839679   0000428032   Unallocated
```

#### Определение ОС по типу раздела

| Тип раздела   | Код        | Вероятная ОС                     |
|---------------|------------|----------------------------------|
| NTFS          | 0x07       | Windows                          |
| FAT32         | 0x0b / 0x0c| Windows / флешки                 |
| ext4          | 0x83       | Linux (включая Astra)            |
| APFS          | 0x00       | macOS                            |
| Linux LVM     | 0x8e       | Linux (серверные системы)        |

### Шаг 1.3: Быстрый поиск сигнатур ОС

```bash
# Поиск сигнатур Windows
strings disk.img | grep -i "microsoft windows" -m 3

# Поиск сигнатур Linux/Astra
strings disk.img | grep -i "astra\|debian\|ubuntu\|linux kernel" -m 3

# Поиск пользовательских папок (универсально)
strings disk.img | grep -iE "Users\\|home/|Documents and Settings" -m 5
```

> 💡 **Совет:** Первые 3–5 минут расследования потратьте ТОЛЬКО на определение ОС. Ошибка здесь = потеря времени.

---

## 💽 Этап 2: Извлечение файловой системы

### Вариант A: Монтирование образа (только в режиме read-only!)

#### Для Linux-образов:

```bash
# Установка необходимых пакетов
sudo apt install libguestfs-tools

# Автоматическое монтирование через guestfish
guestmount -a disk.img -m /dev/sda1 --ro /mnt/forensics

# Если не определился раздел — список разделов:
guestfish -a disk.img -i "run" "list-filesystems"
```

#### Для Windows-образов:

```bash
# Установка
sudo apt install libguestfs-tools

# Монтирование NTFS-раздела
guestmount -a disk.img -m /dev/sda1 --ro /mnt/forensics
```

> ⚠️ **КРИТИЧЕСКИ ВАЖНО:** Флаг `--ro` (read-only) обязателен! Без него вы можете изменить образ.

### Вариант B: Анализ без монтирования (рекомендуется для чистоты)

Используйте инструменты из пакета Sleuth Kit:

```bash
# Список файлов в корне раздела 1 (смещение 2048 секторов)
fls -r -o 2048 disk.img 1

# Просмотр содержимого файла по inode (например, 12345)
icat -o 2048 disk.img 12345

# Поиск удалённых файлов
fls -d -o 2048 disk.img 1
```

> 💡 **Почему без монтирования?** Некоторые ОС при монтировании создают временные файлы (например, `.Trash-1000` в Linux), что нарушает целостность.

---

## 🧩 Этап 3: Сбор артефактов (по ОС)

### Windows

#### 3.1. Реестр Windows (самый богатый источник!)

| Hive         | Расположение в образе                                      | Что содержит                          | Инструмент                              |
|--------------|------------------------------------------------------------|---------------------------------------|-----------------------------------------|
| SYSTEM       | Windows/System32/config/SYSTEM                             | Подключённые USB-устройства, сетевые настройки | `regripper -r SYSTEM -p usbstor`        |
| SOFTWARE     | Windows/System32/config/SOFTWARE                           | Установленные программы, автозагрузка | `regripper -r SOFTWARE -p uninstall`    |
| SAM          | Windows/System32/config/SAM                                | Локальные пользователи (хеши паролей) | `samdump2 SYSTEM SAM`                   |
| NTUSER.DAT   | Users/\<user\>/NTUSER.DAT                                  | Настройки пользователя, недавние документы | `regripper -r NTUSER.DAT -p recentdocs` |
| USRCLASS.DAT | Users/\<user\>/AppData/Local/Microsoft/Windows/USRCLASS.DAT| Shellbags (посещённые папки)          | `regripper -r USRCLASS.DAT -p shellbags`|

Как извлечь hive-файлы без монтирования:

```bash
# Извлечение SYSTEM через icat
icat -o 2048 disk.img $(ifind -n /Windows/System32/config/SYSTEM disk.img) > SYSTEM

# Анализ через RegRipper
rip.pl -r SYSTEM -f system > system_report.txt
```

#### 3.2. Prefetch и Amcache

```bash
# Анализ Prefetch через PECmd (Eric Zimmerman)
PECmd.exe -d /mnt/forensics/Windows/Prefetch/

# Анализ Amcache
AmcacheParser.exe -f Amcache.hve -t amcache.csv
```

#### 3.3. История браузеров

| Браузер | Расположение                                                                 | Инструмент                              |
|---------|------------------------------------------------------------------------------|-----------------------------------------|
| Chrome  | Users/\<user\>/AppData/Local/Google/Chrome/User Data/Default/               | ChromeHistoryView.exe                   |
| Edge    | Users/\<user\>/AppData/Local/Microsoft/Edge/User Data/Default/              | BrowsingHistoryView.exe                 |
| Firefox | Users/\<user\>/AppData/Roaming/Mozilla/Firefox/Profiles/*.default/          | `sqlite3 places.sqlite "SELECT * FROM moz_places"` |

#### 3.4. $MFT и анализ удалённых файлов

```bash
# Извлечение $MFT
icat -o 2048 disk.img 0 > mft.raw

# Анализ через MFTECmd
MFTECmd.exe -f mft.raw --csv mft_analysis/

# Поиск удалённых записей
mftdump mft.raw | grep -i "deleted"
```

### Linux / Astra

#### 3.5. Системные логи

| Лог         | Расположение           | Что содержит                              |
|-------------|------------------------|-------------------------------------------|
| auth.log    | /var/log/auth.log      | Входы по SSH, sudo, неудачные попытки     |
| syslog      | /var/log/syslog        | Общие системные события                   |
| kern.log    | /var/log/kern.log      | События ядра                              |
| journal     | /var/log/journal/      | Системный журнал systemd                  |

Как извлечь без монтирования:

```bash
# Поиск файла в образе
fls -r -o 2048 disk.img | grep "auth.log"

# Извлечение по inode (например, inode 123456)
icat -o 2048 disk.img 123456 > auth.log
```

#### 3.6. История пользователя

| Файл                | Расположение                     | Содержание                              |
|---------------------|----------------------------------|-----------------------------------------|
| .bash_history       | /home/\<user\>/.bash_history     | Команды в терминале                     |
| .zsh_history        | /home/\<user\>/.zsh_history      | Команды в Zsh                           |
| .ssh/known_hosts    | /home/\<user\>/.ssh/known_hosts  | Подключённые хосты по SSH               |
| .ssh/id_rsa         | /home/\<user\>/.ssh/id_rsa       | Приватный ключ (если не защищён паролем)|

#### 3.7. Сетевые артефакты

```bash
# Статические настройки
cat /etc/network/interfaces
cat /etc/netplan/*.yaml

# Хосты и DNS
cat /etc/hosts
cat /etc/resolv.conf

# Последние подключения
last -f /var/log/wtmp
```

#### 3.8. Менеджер пакетов

```bash
# Debian/Ubuntu/Astra
cat /var/log/apt/history.log
grep "install" /var/log/dpkg.log

# RHEL/CentOS
cat /var/log/yum.log
```

---

## 🕳️ Этап 4: Поиск скрытых и удалённых данных

### 4.1. Скрытые разделы

```bash
# Анализ через TestDisk (интерактивный режим)
testdisk disk.img

# Автоматический поиск через mmls
mmls disk.img

# Поиск сигнатур разделов вручную
dd if=disk.img bs=512 skip=63 count=1 | xxd | head -5
```

### 4.2. Следы удалённых файлов

```bash
# Photorec — восстановление по сигнатурам
photorec disk.img

# Foremost — аналогично
foremost -i disk.img -o recovered_files/

# Анализ слотов в файловой системе
fls -d -o 2048 disk.img 1  # для раздела 1
```

### 4.3. Стеганография

```bash
# Анализ изображений
binwalk image.jpg
steghide extract -sf image.jpg  # запросит пароль
zsteg image.png                 # для PNG с прозрачностью

# Анализ аудио/видео
stegseek audio.wav wordlist.txt  # brute-force пароля

# Анализ архивов внутри файлов
binwalk -e suspicious.docx
```

### 4.4. Поиск по ключевым словам (флаги!)

```bash
# Быстрый поиск флагов в формате RS{...}
strings disk.img | grep -iE "RS\{[a-zA-Z0-9_\-]+\}" -A 2 -B 2

# Расширенный поиск с контекстом
grep -a -i -E "flag|secret|password|credential" disk.img -C 5 | head -100

# Поиск в определённых кодировках
strings -e l disk.img | grep "RS{"  # little-endian (Windows)
strings -e b disk.img | grep "RS{"  # big-endian (редко)
```

---

## ⏱️ Этап 5: Построение таймлайна событий

Таймлайн — хронологическая последовательность событий. Критически важен для понимания инцидента.

### 5.1. Создание таймлайна через Plaso (log2timeline)

```bash
# Установка
sudo apt install plaso-tools

# Генерация таймлайна
log2timeline.py timeline.plaso disk.img

# Экспорт в CSV для анализа
psort.py -w timeline.csv timeline.plaso
```

### 5.2. Анализ через mactime (Sleuth Kit)

```bash
# Создание тела для mactime
fls -m / -r -o 2048 disk.img 1 > body.txt

# Генерация таймлайна за последние 24 часа
mactime -b body.txt -d -z UTC | grep "$(date -d 'yesterday' +%Y-%m-%d)"
```

### 5.3. Ключевые временные метки для поиска

| Событие                     | Где искать                   | Почему важно                     |
|-----------------------------|------------------------------|----------------------------------|
| Первый вход злоумышленника  | auth.log, Security.evtx      | Точка компрометации              |
| Запуск вредоносной программы| Prefetch, Amcache, .bash_history | Действия атакующего           |
| Кража данных                | Сетевые логи, История браузера | Эксплуатация                    |
| Очистка следов              | Очищенные логи, Удалённые файлы | Признаки профессионализма      |

---

## 🌐 Этап 6: Сетевая форензика

### 6.1. Статические настройки

```bash
# Windows
regripper -r SYSTEM -p networkcards
regripper -r SOFTWARE -p networkprofiles

# Linux
cat /etc/network/interfaces
cat /etc/hosts
cat ~/.ssh/config
```

### 6.2. Кэш ARP и маршрутизация

```bash
# Windows (из дампа памяти или реестра)
regripper -r SYSTEM -p arp

# Linux
cat /proc/net/arp        # если есть живая система
cat /etc/hosts.allow
```

### 6.3. История подключений

```bash
# Windows
regripper -r NTUSER.DAT -p typedurls    # набранные URL
strings pagefile.sys | grep "http"      # кэш в файле подкачки

# Linux
last -f /var/log/wtmp    # успешные входы
lastb -f /var/log/btmp   # неудачные попытки
grep "Accepted" /var/log/auth.log
```

---

## 💾 Этап 7: Анализ памяти (если есть дамп)

> ⚠️ Обычно в базовых модулях дампа памяти нет. Этот раздел для полноты картины.

```bash
# Определение профиля ОС
volatility -f memory.dump imageinfo

# Список процессов
volatility -f memory.dump --profile=Win7SP1x64 pslist

# Сетевые соединения
volatility -f memory.dump --profile=Win7SP1x64 netscan

# Дамп процесса по PID
volatility -f memory.dump --profile=Win7SP1x64 memdump -p <PID> -D .
```

---

## 🔌 Этап 8: Работа с флеш-накопителями (бонусные задания)

### 8.1. Создание образа флешки (ОБЯЗАТЕЛЬНО!)

```bash
# Linux (замените /dev/sdX на реальное устройство!)
sudo dd if=/dev/sdX of=usb.img bs=4M status=progress conv=sync,noerror

# Проверка целостности
sha256sum usb.img > usb.img.sha256
```

> ⚠️ **НИКОГДА не работайте напрямую с флешкой! Только с образом.**

### 8.2. Методы маскировки данных и способы обнаружения

| Метод маскировки       | Как обнаружить                        | Как извлечь                                              |
|------------------------|---------------------------------------|----------------------------------------------------------|
| Скрытый раздел         | `testdisk usb.img`, `fdisk -l usb.img`| Восстановить через testdisk → Write                      |
| Переименованный архив  | `file usb.img`, `binwalk usb.img`     | `mv usb.img archive.zip && unzip archive.zip`            |
| Данные после EOF       | `binwalk -e usb.img`, `dd if=usb.img bs=1 skip=123456` | Извлечь «хвост» через `dd`              |
| Стеганография в изображениях | `zsteg image.png`, `stegsolve.jar` | `steghide extract -sf image.jpg`                         |
| LSB (младшие биты)     | `stegsolve.jar`, `zsteg`              | Автоматическое извлечение инструментами                  |
| Шифрование             | `file container.enc`, `binwalk container.enc` | Brute-force через `stegseek`, `john`            |

### 8.3. Структура отчёта для бонусного задания

Следуйте шаблону из легенды:

```
А. Какая информация была скомпрометирована? Как её можно использовать?
→ Укажите тип данных (пароли, ключи доступа, персональные данные) и потенциальный ущерб.

Б. Приведите скриншот, на котором видна скомпрометированная информация:
→ Скриншот с выделенным флагом/файлом + хеш файла (sha256sum).

В. Какими способами были замаскированы украденные данные, как их можно восстановить?
→ Опишите метод маскировки + команду для извлечения (пример: binwalk -e secret.jpg).

Г. Что же нам теперь делать с точки зрения мер ИБ?
→ Рекомендации по устранению уязвимостей и предотвращению подобных инцидентов.
```

---

## 📝 Этап 9: Оформление отчёта

### 9.1. Требования к отчёту

- **Формат:** ODT (LibreOffice Writer)
- **Имя файла:** `ОТЧЁТ.odt`
- **Местоположение:** Рабочий стол физического компьютера
- **Структура:** Следуйте шаблону из легенды соревнования

### 9.2. Что включить в отчёт

| Раздел              | Содержание                                              | Пример                                                                 |
|---------------------|---------------------------------------------------------|------------------------------------------------------------------------|
| Введение            | Цель расследования, тип образа                          | «Анализ образа RS2026_F.vdi для поиска доказательств компрометации»   |
| Методология         | Инструменты и команды                                   | `sha256sum disk.img`, `mmls disk.img`                                  |
| Ход расследования   | Пошагово с доказательствами                             | Скриншот вывода regripper с выделенным ключевым артефактом            |
| Найденные артефакты | Таблица с путями, хешами, выводами                      | `| /path/to/file | sha256:abc123 | Вывод: ... |`                        |
| Выводы              | Ответы на вопросы легенды                               | «Компрометация произошла через SSH 01.02.2026 в 14:23»                 |

### 9.3. Критические ошибки при оформлении

- ❌ Отчёт сохранён на рабочем столе виртуальной машины
- ❌ Файл назван `отчет.odt` (кириллица в расширении) или `report.odt`
- ❌ Нет хешей файлов-доказательств
- ❌ Скриншоты без подписей и выделения ключевых элементов

### 9.4. Советы по работе с LibreOffice

Включите автосохранение:

**Сервис → Параметры → Загрузка/сохранение → Интервал автосохранения: 2 минуты**

Перед закрытием — экспортируйте резервную копию в PDF:

**Файл → Экспортировать как → Экспортировать как PDF**

Проверьте наличие файла за 10 минут до окончания модуля!

---

## ✅ Чек-листы и шпаргалки

### Чек-лист: Первые 10 минут расследования

- [ ] Сверил хеш оригинального образа с эталоном
- [ ] Создал резервную копию (`cp образ.vdi образ_work.vdi`)
- [ ] Конвертировал в RAW (`qemu-img convert -O raw ...`)
- [ ] Определил тип ОС через `mmls` и `strings`
- [ ] Запустил поиск флагов через `strings | grep "RS{"`
- [ ] Создал папку для отчёта и включил автосохранение в LibreOffice

### Шпаргалка: Быстрые команды по ОС

#### Универсальные команды

```bash
# Поиск флагов
strings образ.img | grep -iE "RS\{[a-zA-Z0-9_\-]+\}"

# Анализ структуры
mmls образ.img
fls -r -o смещение образ.img

# Извлечение файла по пути
icat -o смещение образ.img $(ifind -n "/путь/к/файлу" образ.img)
```

#### Windows-специфичные

```bash
# Анализ реестра
rip.pl -r SOFTWARE -f software > software.txt
PECmd.exe -d C:/Windows/Prefetch/

# Поиск в реестре
regripper -r NTUSER.DAT -p recentdocs | grep -i "secret"
```

#### Linux-специфичные

```bash
# Анализ логов
grep "Failed password" /var/log/auth.log
last -f /var/log/wtmp

# Поиск интересных файлов
find /home -name "*.txt" -o -name "*.log" | xargs grep "password" 2>/dev/null
```

---

## 🛠️ Рекомендуемые инструменты

### Базовый набор (установите заранее!)

| Инструмент           | Назначение                                      | Установка                                      |
|----------------------|-------------------------------------------------|------------------------------------------------|
| Sleuth Kit + Autopsy | Анализ образов, GUI для Sleuth Kit              | `sudo apt install sleuthkit autopsy`           |
| RegRipper            | Анализ реестра Windows                          | `git clone https://github.com/obsidianforensics/rekall` |
| Eric Zimmerman Tools | Prefetch, Amcache, браузеры                     | Скачать с GitHub                               |
| Volatility 3         | Анализ памяти                                   | `pip install volatility3`                      |
| binwalk              | Поиск встроенных файлов, стеганография          | `sudo apt install binwalk`                     |
| testdisk / photorec  | Восстановление разделов и файлов                | `sudo apt install testdisk`                    |
| plaso (log2timeline) | Построение таймлайнов                           | `sudo apt install plaso-tools`                 |

### Расширенный набор

| Инструмент      | Назначение                                              |
|-----------------|---------------------------------------------------------|
| FTK Imager      | Просмотр образов, экспорт артефактов (Windows GUI)      |
| Bulk Extractor  | Быстрый поиск карт платежных систем, email, URL         |
| stegsolve       | Анализ стеганографии в изображениях (Java)              |
| Wireshark       | Анализ сетевого трафика (если есть PCAP)                |
| KAPE            | Автоматизация сбора артефактов (коммерческий, но есть бесплатная версия) |

---

## 💡 Заключение: 5 правил победителя

1. **Целостность прежде всего** — никогда не меняйте оригинал.
2. **Документируй каждый шаг** — если не записал, значит не делал.
3. **Системный подход** — анализируй все категории артефактов, не только «очевидные».
4. **Время — твой ресурс** — распределяй его: 70% на основное задание, 30% на бонусное.
5. **Отчёт = результат** — даже идеальное расследование без отчёта = 0 баллов.

---

## 🚀 Удачи на соревнованиях!

Помните: форензика — это не магия, а методология. Следуйте ей — и флаги найдутся сами.

---

**Версия:** 1.0  
**Дата:** 2026  
**Автор:** Студент форензик-модуля
```

Скопируйте этот текст и сохраните в файл с расширением `.md` (например, `forensics_guide.md`). Готово! 🎉