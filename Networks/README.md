# 🌐 Модуль сетевой безопасности — Защита корпоративной ИТ-инфраструктуры

![Корпоративная безопасность](https://img.shields.io/badge/Network_Security-Hardening-blue?logo=linux)
![Уровень сложности](https://img.shields.io/badge/Difficulty-Intermediate%20to%20Advanced-orange)
![Без спойлеров](https://img.shields.io/badge/Spoiler%20Free-%E2%9C%94%EF%B8%8F-brightgreen)
![REA2026](https://img.shields.io/badge/REA2026-2026-ff6b6b)

Методическое руководство по проектированию, настройке и защите корпоративной сетевой инфраструктуры. Материалы содержат **только общую методологию и инструменты** — конкретные решения заданий соревнований, флаги и легенды **НЕ раскрываются**.

> ⚠️ **ВАЖНО:** Все практические навыки должны отрабатываться в авторизованных лабораторных средах. Настройка сетевой безопасности в реальных корпоративных инфраструктурах без письменного разрешения заказчика нарушает законодательство РФ.

---

## 📌 Содержание

- [🎯 Цели модуля](#-цели-модуля)
- [🔍 Этап 1: Анализ топологии и планирование](#-этап-1-анализ-топологии-и-планирование)
- [⚙️ Этап 2: Настройка сетевой связности](#️-этап-2-настройка-сетевой-связности)
- [🛡️ Этап 3: Защита периметра и хостов](#️-этап-3-защита-периметра-и-хостов)
- [🔐 Этап 4: Централизованное управление и аутентификация](#-этап-4-централизованное-управление-и-аутентификация)
- [💾 Этап 5: Хранилища данных и резервное копирование](#-этап-5-хранилища-данных-и-резервное-копирование)
- [📊 Этап 6: Мониторинг и логирование](#-этап-6-мониторинг-и-логирование)
- [📝 Оформление отчёта](#-оформление-отчёта)
- [✅ Чек-листы и шпаргалки](#-чек-листы-и-шпаргалки)
- [🛠️ Рекомендуемые инструменты](#️-рекомендуемые-инструменты)
- [💡 5 принципов безопасной сетевой архитектуры](#-5-принципов-безопасной-сетевой-архитектуры)

---

## 🎯 Цели модуля

По завершении модуля вы сможете:
- Проектировать безопасную сетевую архитектуру с сегментацией зон
- Настраивать динамическую маршрутизацию с аутентификацией (OSPF/BGP)
- Реализовывать защищённые удалённые доступы (VPN, SSH hardening)
- Настраивать централизованную аутентификацию (FreeIPA, LDAP, Kerberos)
- Обеспечивать отказоустойчивость хранилищ данных (NFS, репликация)
- Настраивать механизмы мониторинга и аудита сетевых событий
- Готовить техническую документацию для экспертов и заказчиков

---

## 🔍 Этап 1: Анализ топологии и планирование

### 1.1. Изучение исходной топологии

```bash
# Определение активных интерфейсов
ip link show

# Проверка текущих маршрутов
ip route show

# Анализ соседних устройств (CDP/LLDP)
tcpdump -i eth0 -nn -v ether[20:2]==0x2000 or ether[12:2]==0x88cc
```

### 1.2. Планирование адресного пространства

| Сегмент | Диапазон | Назначение | Примечания |
|---------|----------|------------|------------|
| ЦОД | 10.10.10.0/24 | Серверная инфраструктура | Высокая защита |
| Офис | 10.20.20.0/24 | Рабочие станции | Фильтрация исходящего трафика |
| Управление | 192.168.254.0/24 | Сетевые устройства | Только из зоны управления |

> 💡 **Совет:** Перед настройкой составьте карту сетевых зон и матрицу взаимодействия между ними. Это предотвратит ошибки в маршрутизации и фильтрации.

---

## ⚙️ Этап 2: Настройка сетевой связности

### 2.1. Настройка интерфейсов

```bash
# Пример конфигурации /etc/network/interfaces (Debian)
auto eth0
iface eth0 inet static
    address 10.10.10.1/24
    gateway 10.10.10.254
    post-up iptables-restore < /etc/iptables/rules.v4
```

### 2.2. Динамическая маршрутизация (OSPF)

```bash
# Установка FRRouting (современная замена Quagga)
sudo apt install frr frr-pythontools

# Конфигурация OSPF (/etc/frr/frr.conf)
router ospf
 ospf router-id 10.10.10.1
 network 10.10.10.0/24 area 0
 network 10.20.20.0/24 area 0
 area 0 authentication message-digest
 interface tun0
  ip ospf message-digest-key 1 md5 MySecretKey
  ip ospf hello-interval 10
```

### 2.3. Настройка туннелей (VPN между сайтами)

```bash
# Пример WireGuard (современная альтернатива OpenVPN)
# /etc/wireguard/wg0.conf
[Interface]
Address = 10.255.255.1/30
PrivateKey = <серверный_ключ>
ListenPort = 51820

[Peer]
PublicKey = <публичный_ключ_пира>
AllowedIPs = 10.255.255.2/32, 10.20.20.0/24
Endpoint = cod-rtr.example.com:51820
PersistentKeepalive = 25
```

### 2.4. PAT/NAT для выхода в интернет

```bash
# Настройка через nftables (современная замена iptables)
table ip nat {
    chain postrouting {
        type nat hook postrouting priority 100; policy accept;
        oifname "eth0" ip saddr 10.0.0.0/8 counter masquerade
        oifname "eth0" counter limit rate 100 packets/second accept
        oifname "eth0" counter drop
    }
}
```

---

## 🛡️ Этап 3: Защита периметра и хостов

### 3.1. Усиление SSH

```bash
# /etc/ssh/sshd_config
Port 2222
PermitRootLogin no
AllowUsers sshuser
PasswordAuthentication no
PubkeyAuthentication yes
MaxAuthTries 2
LoginGraceTime 30
ClientAliveInterval 300
ClientAliveCountMax 2

# Интеграция с Fail2ban
# /etc/fail2ban/jail.local
[sshd]
enabled = true
port = 2222
filter = sshd
logpath = /var/log/auth.log
maxretry = 2
bantime = 60
```

### 3.2. Аудит подключений через auditd

```bash
# /etc/audit/rules.d/audit.rules
-w /var/log/auth.log -p wa -k ssh_access
-w /etc/ssh/sshd_config -p wa -k ssh_config
-a always,exit -F arch=b64 -S execve -k exec
```

### 3.3. Мониторинг изменений маршрутов

```bash
#!/bin/bash
# /usr/local/bin/monitor_routes.sh
LOGFILE="/var/log/router_change.log"
PREV_ROUTES="/tmp/prev_routes.txt"

ip route show | sort > /tmp/current_routes.txt

if ! cmp -s /tmp/current_routes.txt $PREV_ROUTES 2>/dev/null; then
    echo "$(date '+%Y-%m-%d %H:%M:%S') - ROUTE CHANGE DETECTED" >> $LOGFILE
    diff $PREV_ROUTES /tmp/current_routes.txt >> $LOGFILE 2>&1
    
    # Уведомление в активные сессии
    wall "WARNING: Network routes have been reconfigured at $(date)"
    
    cp /tmp/current_routes.txt $PREV_ROUTES
fi

# Добавить в cron для периодического запуска
# */1 * * * * /usr/local/bin/monitor_routes.sh
```

---

## 🔐 Этап 4: Централизованное управление и аутентификация

### 4.1. Развёртывание FreeIPA

```bash
# Установка сервера FreeIPA
sudo apt install freeipa-server freeipa-server-dns

# Инициализация (выполняется один раз)
ipa-server-install \
  --domain=reaskills.cyber \
  --realm=REASKILLS.CYBER \
  --admin-password='P@ssw0rd2026' \
  --ds-password='DirectoryManagerPass' \
  --setup-dns \
  --no-forwarders \
  --auto-reverse
```

### 4.2. Парольная политика

```bash
# Настройка через IPA CLI
ipa pwpolicy-mod --minlength=10 \
  --history=4 \
  --maxlife=31 \
  --minclasses=3 \
  --maxfail=3 \
  --failinterval=900 \
  --lockouttime=900

# Предупреждение об истечении пароля
ipa config-mod --pwdexpnotify 7
```

### 4.3. Интеграция клиентов

```bash
# На клиентской машине (PC1)
ipa-client-install \
  --domain=reaskills.cyber \
  --server=main-dc1.reaskills.cyber \
  --principal=admin \
  --password='P@ssw0rd2026' \
  --mkhomedir \
  --force-join
```

### 4.4. Настройка сертификатного центра

```bash
# Хранение сертификатов в едином месте
mkdir -p /etc/ca/{certs,private,crl,newcerts}
chmod 700 /etc/ca/private
touch /etc/ca/index.txt
echo 1000 > /etc/ca/serial

# Пример генерации сертификата для веб-сервера
openssl req -new -newkey rsa:2048 -nodes \
  -keyout /etc/ca/private/web.key \
  -out /etc/ca/certs/web.csr \
  -subj "/C=RU/ST=Moscow/L=Moscow/O=REA2026/CN=www.reaskills.cyber"

openssl ca -config /etc/ca/openssl.cnf \
  -in /etc/ca/certs/web.csr \
  -out /etc/ca/certs/web.crt \
  -extensions server_ext
```

---

## 💾 Этап 5: Хранилища данных и резервное копирование

### 5.1. Настройка NFS с безопасностью

```bash
# /etc/exports на MAIN-STORAGE1
/data 10.20.20.0/24(rw,sync,no_subtree_check,root_squash,sec=sys)

# Монтирование на клиентах
echo "main-storage1:/data /mnt/home nfs rw,soft,intr 0 0" >> /etc/fstab
```

### 5.2. Автоматизированное резервное копирование

```bash
#!/bin/bash
# /usr/local/bin/backup_data.sh
SOURCE="/data"
DEST="main-storage2:/data-backup"
DATE=$(date +%Y%m%d)
ARCHIVE="backup_$DATE.tar.gz.gpg"

# Создание зашифрованного архива
tar -czf - $SOURCE | \
  gpg --symmetric --cipher-algo AES256 --passphrase 'BackupPa$$' -o /tmp/$ARCHIVE

# Копирование на удалённый сервер
scp /tmp/$ARCHIVE $DEST/

# Очистка временных файлов
rm /tmp/$ARCHIVE

# Логирование
echo "[$(date)] Backup completed successfully" >> /var/log/backup.log
```

### 5.3. Веб-сервер с аутентификацией (Nginx + FreeIPA)

```nginx
# /etc/nginx/sites-available/secure-storage
server {
    listen 443 ssl;
    server_name www.reaskills.cyber;
    
    ssl_certificate /etc/ca/certs/web.crt;
    ssl_certificate_key /etc/ca/private/web.key;
    
    location / {
        auth_request /auth;
        root /data_saver;
        autoindex on;
    }
    
    location = /auth {
        internal;
        proxy_pass http://main-dc1.reaskills.cyber:8080;
        proxy_pass_request_body off;
        proxy_set_header Content-Length "";
        proxy_set_header X-Original-URI $request_uri;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

---

## 📊 Этап 6: Мониторинг и логирование

### 6.1. Централизованное логирование

```bash
# На клиенте (PC1) - /etc/rsyslog.d/10-forward.conf
*.* action(type="omfwd" target="main-dc1.reaskills.cyber" port="514" protocol="tcp")

# На сервере (MAIN-DC1) - /etc/rsyslog.d/10-listen.conf
module(load="imtcp")
input(type="imtcp" port="514")
```

### 6.2. Аудит XRDP-сессий

```bash
# /etc/xrdp/sesman.ini
[Security]
AllowRootLogin=false
MaxLoginRetry=3
TerminalServerUsers=tsusers
TerminalServerAdmins=tsadmins

# Скрипт для логирования подключений
# /etc/xrdp/xrdp-monitor.sh
#!/bin/bash
echo "$(date) - User $1 connected from $2" >> /var/log/xrdp_audit.log
logger -t xrdp-audit "User $1 connected from $2"
```

---

## 📝 Оформление отчёта

### Требования к отчёту
- **Формат:** ODT (LibreOffice Writer) или PDF
- **Имя файла:** `ОТЧЁТ_СЕТИ.odt`
- **Местоположение:** Рабочий стол физического компьютера
- **Структура:**
  1. Краткое резюме выполненных работ
  2. Схема сетевой топологии с зонами безопасности
  3. Конфигурационные файлы ключевых сервисов (с комментариями)
  4. Результаты проверки работоспособности (скриншоты + хеши)
  5. Выявленные уязвимости и рекомендации по устранению

### Критические ошибки при оформлении
- ❌ Отсутствие хешей конфигурационных файлов
- ❌ Скриншоты без выделения ключевых элементов
- ❌ Нет доказательств работоспособности сервисов (например, `curl -I https://...`)
- ❌ Отчёт сохранён на рабочем столе виртуальной машины

---

## ✅ Чек-листы и шпаргалки

### Чек-лист: Первые 30 минут работы
- [ ] Изучил топологию и составил карту сетевых зон
- [ ] Определил диапазоны адресов для каждого сегмента
- [ ] Проверил базовую связность между устройствами (`ping`, `traceroute`)
- [ ] Составил план последовательности настройки (от физического уровня к прикладному)
- [ ] Создал резервные копии исходных конфигураций всех устройств

### Шпаргалка: Быстрые проверки работоспособности

```bash
# Проверка маршрутизации
ip route get 10.20.20.50

# Проверка работы OSPF
vtysh -c "show ip ospf neighbor"

# Проверка сертификатов
openssl x509 -in /etc/ca/certs/web.crt -text -noout | grep -E "Subject:|Issuer:|Not After"

# Проверка правильности монтирования NFS
df -h | grep nfs

# Проверка работы веб-сервера с аутентификацией
curl -k -u user:password https://www.reaskills.cyber/

# Проверка логов аутентификации
tail -f /var/log/auth.log | grep -i "accepted\|failed"
```

### Шпаргалка: Распространённые ошибки и их решение

| Симптом | Возможная причина | Решение |
|---------|-------------------|---------|
| Нет связи между сегментами | Отсутствует маршрут или неправильный шлюз | `ip route add ...` или проверка OSPF |
| Не работает аутентификация | Неправильные права на сертификаты | `chmod 644 *.crt && chmod 600 *.key` |
| Таймауты при подключении | Блокировка фаерволом | `nft list ruleset \| grep <порт>` |
| Ошибки сертификатов в браузере | Не добавлен в доверенные | Импорт через `certutil` или настройки браузера |
| Не монтируется NFS | Отсутствует экспорт или неправильные права | Проверка `/etc/exports` и `exportfs -ra` |

---

## 🛠️ Рекомендуемые инструменты

### Базовый набор

| Инструмент | Назначение | Установка |
|------------|------------|-----------|
| FRRouting | Динамическая маршрутизация (OSPF, BGP) | `sudo apt install frr` |
| nftables | Современный фаервол (замена iptables) | Входит в ядро Linux ≥4.18 |
| Fail2ban | Защита от брутфорса | `sudo apt install fail2ban` |
| auditd | Система аудита | `sudo apt install auditd` |
| FreeIPA | Централизованная аутентификация | `sudo apt install freeipa-server` |
| WireGuard | Современный VPN | `sudo apt install wireguard` |

### Расширенный набор

| Инструмент | Назначение |
|------------|------------|
| Rsyslog + ELK | Централизованное логирование и анализ |
| Zabbix / Prometheus | Мониторинг сетевых устройств |
| HashiCorp Vault | Управление секретами и сертификатами |
| Ansible | Автоматизация развёртывания конфигураций |
| Lynis | Аудит безопасности хостов |

---

## 💡 5 принципов безопасной сетевой архитектуры

1. **Сегментация прежде всего** — разделяйте сети по принципу минимальных привилегий (принцип нулевого доверия).
2. **Защита на всех уровнях** — не полагайтесь только на периметральную защиту; применяйте защиту на хостах и приложениях.
3. **Аудит всего критичного** — логируйте все события аутентификации, изменения конфигураций и доступ к чувствительным данным.
4. **Автоматизация рутины** — используйте скрипты и инструменты управления конфигурациями для исключения человеческих ошибок.
5. **Документирование решений** — каждая настройка должна иметь обоснование в документации для будущих аудитов.

---

## 📜 Этические принципы

- ✅ Все настройки выполняются ТОЛЬКО в рамках авторизованных лабораторных сред
- ✅ Использование стандартных паролей (`P@ssw0rd`) допустимо ТОЛЬКО в учебных целях
- ❌ Запрещено применение полученных навыков для несанкционированного доступа к сетям
- ❌ Запрещено распространение конфигураций с реальными учётными данными

---

> 🔒 «Безопасность сети — это процесс, а не продукт» — Брюс Шнайер

---

**Версия:** 1.0  
**Дата:** 2026  
**Автор:** Кругленко Олег Кириллович (РТУ МИРЭА)  
**Модуль:** Защита корпоративной ИТ-инфраструктуры (REA2026)
